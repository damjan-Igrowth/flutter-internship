import 'package:flutter/material.dart';

class ComponentsClass extends StatelessWidget {
  const ComponentsClass({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
