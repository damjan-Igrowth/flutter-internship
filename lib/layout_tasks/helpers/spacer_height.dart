import 'package:flutter/material.dart';

class SpacerHeight extends StatelessWidget {
  const SpacerHeight({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 40);
  }
}
